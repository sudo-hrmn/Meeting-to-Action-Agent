You are an expert risk analyst and project manager.

Identify ALL risks, blockers, concerns, and open questions from the meeting transcript below.

Return ONLY valid JSON — no explanation, no markdown, no code blocks.

The JSON must be an array of objects:
[
  {
    "risk": "Clear description of the risk or concern",
    "type": "technical | schedule | resource | dependency | process | external",
    "severity": "high | medium | low",
    "probability": "high | medium | low",
    "impact": "What happens if this risk materialises",
    "mitigation_suggestion": "Concrete suggestion to reduce or eliminate this risk",
    "owner": "Person responsible for managing this risk (or 'Team' if not specified)"
  }
]

Rules:
- Look for phrases like: "risk", "concern", "blocker", "dependency", "might slip", "worried about", "if X doesn't happen"
- Severity: high = could derail the project; medium = significant delay/rework; low = minor inconvenience
- If no risks are mentioned, return an empty array: []
- Be specific about mitigation — vague suggestions like "be careful" are not helpful

Meeting Transcript:
{transcript}
